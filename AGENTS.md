# Agent 协作规范

本文件是本仓库中所有 AI Agent 和协作成员的工作入口。开始工作前先阅读本文件、`hand_off.md`、`计算成果/计算进度.md` 和 `资料索引/阅读索引.md`。

## 1. 工作原则

- 一次只推进一个计算项目；当前项目未通过用户审核前，不得自动开始下一个项目。
- 先复用已有成果和资料索引，不重复扫描整本手册，也不凭记忆补写设计输入。
- 区分“题设原始数据”“已通过口径”“工程假定”“待补数据”，任何假定都要写出依据、影响和复核点。
- 不覆盖、重命名或删除原始资料；新增缓存、摘录和核对图放入 `资料索引/` 对应目录。
- 不把其他题目的算例数据当成本题输入；不确定时标记为“待确认”，不要猜测。

## 2. 开始任务前

1. 阅读 `hand_off.md`，确认当前交接点和项目边界。
2. 阅读 `计算成果/计算进度.md`，确认已通过和未完成项目。
3. 阅读 `资料索引/阅读索引.md`，按页码定位原始资料。
4. 检查 `git status`，避免覆盖他人的未提交修改。
5. 在开始修改前，在任务说明或 Issue 中写明：目标文件、输入依据、预计产出和待审核事项。

## 3. 成果文件规范

每个计算成果按以下顺序组织：

1. 参数及依据；
2. 公式与符号；
3. 数值代入；
4. 取整或设备选择；
5. 工况校核；
6. 结果表、流程图或草图；
7. 待审核事项。

引用资料时尽量同时写明文件名、书内页码、PDF 页码、表号或条款。Markdown 文件名使用中文、项目编号和状态，例如 `计算成果/03_工艺方案与设计参数确定_待审核.md`。

## 4. 修改边界

- Agent 只修改当前任务明确授权的文件。
- 不修改已通过项目的主算口径，除非用户明确要求复核；如确需修正，必须在文档中保留变更原因和影响。
- 不提前计算后续项目的单体尺寸、池容、栅隙数量、管渠水力等内容。
- 不擅自删掉任务书明确要求的构筑物。
- 不提交个人密钥、Token、Cookie、完整本机路径、临时文件或构建产物。

## 5. 交接与审核

- 任务完成后更新 `计算成果/计算进度.md`，并在成果文件末尾列出“待审核事项”。
- 回复中说明：改动文件、主要结论、关键假定、验证方式和需要用户确认的问题。
- 用户未明确通过前，状态保持“待审核”，不得写成“已完成”或据此推进下一项。
- 交接给下一 Agent 时，优先更新 `hand_off.md`，写清当前项目、已验证内容、未决问题和下一步唯一动作。

## 6. Git 协作

- 分支命名建议：`agent/<项目编号>-<简短主题>`，例如 `agent/03-process-selection`。
- 提交信息使用清晰的动词开头，例如 `Add process selection draft`、`Update calculation assumptions`。
- 一个提交尽量只对应一个逻辑任务；不要把无关格式化混入成果提交。
- 推送前执行：`git status`、Markdown 链接检查、文件存在性检查和必要的计算复核。
- 优先提交 Pull Request，描述中包含变更范围、依据、验证结果和待审核问题。
- 遇到冲突先保留双方内容并人工判断，不使用强制推送覆盖他人分支；不执行 `git reset --hard` 或批量删除。

## 7. 最低验证清单

- 目标文件存在且 Markdown 可正常阅读；
- 引用的文件路径和页码可追溯；
- 公式、单位、数量级和取整逻辑自洽；
- 没有引入未经说明的输入或结论；
- `git diff --check` 无明显空白错误；
- 最终回复明确列出仍需用户审核的事项。


# 协作指南

## 提交任务

请在 Issue 或任务描述中说明：

- 要完成的计算项目和目标文件；
- 使用的题设数据、已通过口径和资料页码；
- 预期交付物；
- 需要后续闭合的假定或可能阻塞自动合并的风险。

## Pull Request

PR 标题建议使用：`[项目03] 确定工艺方案与设计参数`。

PR 描述至少包含：

1. 做了什么；
2. 改了哪些文件；
3. 采用了哪些假定；
4. 如何验证；
5. 哪些假定仍需在后续项目复核。

每个项目完成后创建独立 PR。Codex 基于最新 `main` 自审；验证通过且无冲突时直接合并，不等待用户逐项批准。历史文件名中的“待审核”可保留以维持链接稳定。

## Review 重点

审阅者重点检查数据来源、单位、公式、边界条件、回流关系、前后项目接口，以及是否误用其他题目的算例数据。

出现文本或二进制冲突、必需检查失败、敏感信息、不可逆操作，或足以使当前成果失效的高风险问题时，不得自动合并；其余已记录且可在后续项目闭合的工程假定不阻塞合并。

